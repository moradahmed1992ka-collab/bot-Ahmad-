import logging from telegram import Update from telegram.ext import ( ApplicationBuilder, CommandHandler, MessageHandler, ContextTypes, filters, ) # ضع التوكن الخاص بك هنا BOT_TOKEN = "8516492517:AAFUxj3UzZ9ymXyTmyd3F8wSz3Vf8jlgMxM" # إعداد السجل logging.basicConfig( format="%(asctime)s - %(name)s - %(levelname)s - %(message)s", level=logging.INFO ) # --- الأوامر --- async def start(update: Update, context: ContextTypes.DEFAULT_TYPE): """رسالة الترحيب عند كتابة /start""" user = update.effective_user await update.message.reply_text( f"مرحباً {user.first_name}! 👋\n" "أنا بوت تيليغرام بسيط.\n\n" "الأوامر المتاحة:\n" "/start - بدء المحادثة\n" "/help - المساعدة\n" "/about - معلومات عن البوت" ) async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE): """رسالة المساعدة عند كتابة /help""" await update.message.reply_text( "📌 الأوامر المتاحة:\n\n" "/start - بدء المحادثة\n" "/help - عرض هذه الرسالة\n" "/about - معلومات عن البوت\n\n" "يمكنك أيضاً إرسال أي رسالة نصية وسأرد عليها!" ) async def about(update: Update, context: ContextTypes.DEFAULT_TYPE): """معلومات عن البوت""" await update.message.reply_text( "🤖 هذا بوت تيليغرام تم إنشاؤه بـ Python\n" "المكتبة المستخدمة: python-telegram-bot\n" "الإصدار: 20+" ) # --- معالج الرسائل النصية --- async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE): """الرد على أي رسالة نصية""" text = update.message.text await update.message.reply_text( f"📨 استلمت رسالتك:\n«{text}»\n\n" "سأعمل على معالجتها قريباً! 😊" ) # --- تشغيل البوت --- def main(): app = ApplicationBuilder().token(BOT_TOKEN).build() # تسجيل الأوامر app.add_handler(CommandHandler("start", start)) app.add_handler(CommandHandler("help", help_command)) app.add_handler(CommandHandler("about", about)) # تسجيل معالج الرسائل app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message)) print("✅ البوت يعمل الآن... اضغط Ctrl+C للإيقاف") app.run_polling() if __name__ == "__main__": main()
